import Rebase from 're-base';

const base = Rebase.createClass({
  // Initialize Firebase
    apiKey: "YOUR_FIREBASE_APIKEY",
    authDomain: "YOUR _FIREBASE_DOMAIN",
    databaseURL: "YOUR_FIREBASE_DATABASE"
});

export default base;
